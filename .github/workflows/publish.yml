name: Tag name CD

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@master

      - name: Build 🏗️
        run: echo ${GIHUB_REF}
